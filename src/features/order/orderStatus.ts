export enum OrderStatus {
  pending = "pending",
  offlinePayment = "offlinePayment",
  confirmed = "confirmed",
  shipped = "shipped",
  delivered = "delivered",
  canceled = "canceled",
}

export enum OrderLineStatus {
  pending = "pending",
  offlinePayment = "offlinePayment",
  confirmed = "confirmed",
  shipped = "shipped",
  delivered = "delivered",
  canceled = "canceled",
}
