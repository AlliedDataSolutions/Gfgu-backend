export enum OrderStatus {
  pending = "pending",
  confirmed = "confirmed",
  shipped = "shipped",
  delivered = "delivered",
  canceled = "canceled",
}

export enum OrderLineStatus {
  pending = "pending",
  confirmed = "confirmed",
  shipped = "shipped",
  delivered = "delivered",
  canceled = "canceled",
}
